# Core PDF manipulation
pymupdf>=1.24.0
pymupdf-fonts>=1.0.5   # NotoSans e outras fontes com cobertura Unicode completa

# Free translation backends (no API key required)
deep-translator>=1.11.4

# Web backend
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.3

# Future: paid providers (optional, only if env vars are set)
# anthropic>=0.25.0
# deepl>=1.18.0
